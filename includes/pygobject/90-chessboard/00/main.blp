using Gtk 4.0;
using Adw 1;

ApplicationWindow main_window {
  title: _("Boilerplate");
  default-height: 600;
  default-width: 600;

  Box box {
    orientation: vertical;
    Label label {
      label: "Chessboard";
    }
  }
}
