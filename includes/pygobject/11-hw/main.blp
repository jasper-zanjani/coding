using Gtk 4.0;

ApplicationWindow main_window {
  default-width: 200;
  default-height: 100;

  Box {
    halign: center;
    valign: center;

    Label label {
      label: "Hello, World!";
    }
  }
}
