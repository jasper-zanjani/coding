# Create the skeleton for a new project, creating the named directory
django-admin startproject $PROJECT_NAME

# Create the skeleton for a new project but creating the associated files in the current directory
django-admin startproject $PROJECT_NAME .
