The only requirement for a subclass of AdwApplication is that it override `activate()`.
The simplest possible implementation appears to be this.

```vala
--8<-- "includes/vala/AdwApplication/00/main.vala"
```
